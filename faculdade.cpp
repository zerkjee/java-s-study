#include <stdio.h>
int main(){
    int tam = 15;
    int numeros [tam];
    int maior;
    for (int i = 0;i <15; i++)
{
    printf("Digite %d numero", i+1);
    scanf ("%d", &numeros[i]);
  
    
}
maior = numeros[0];
  for (int i = 0;i <15; i++)
  if(numeros{i}> maior){ maior = numeros[i];}
{ printf("o maior tempo da corrida é: %d", numeros > maior)
return 0;
}
